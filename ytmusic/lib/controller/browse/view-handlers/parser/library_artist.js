'use strict';

const EndpointParser = require(__dirname + '/endpoint');

class LibraryArtistParser extends EndpointParser {
  // Library artist essentially shares the same data as an endpoint item
}

module.exports = LibraryArtistParser;