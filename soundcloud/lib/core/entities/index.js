'use strict';

const Album = require(__dirname + '/album');
const Playlist = require(__dirname + '/playlist');
const Track = require(__dirname + '/track');
const User = require(__dirname + '/user');
const Selection = require(__dirname + '/selection');

module.exports = { Album, Playlist, Track, User, Selection };